# values-local.yaml
environment: local

db:
  host: localhost
  port: 5432
  user: postgres
  password: "" # Use .env

redis:
  host: localhost
  port: 6379

qdrant:
  host: localhost
  port: 6334

neo4j:
  uri: bolt://localhost:7687

minio:
  endpoint: localhost:9000
